from setuptools import setup, find_packages

setup(
    name='my_library',
    version='0.1.0',
    author='Дарья Карцева',
    description='Библиотека пересчета координат из одной системы отсчетав другую',
    packages=find_packages()
)